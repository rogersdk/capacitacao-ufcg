<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ufcg.embedded.threadservice">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ThreadActivity"></activity>





        <receiver android:name=".OpenWifiReceiver">
            <intent-filter>
                <!--<action android:name="android.net.wifi.STATE_CHANGE"/>-->
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED"/>

            </intent-filter>
        </receiver>

        <service android:name=".OpenWifiConnectorService">
            <intent-filter>
                <action android:name="br.edu.ufcg.embedded.threadservice.OPEN_WIFI_CONNECTOR_SERVICE"/>
            </intent-filter>
        </service>

    </application>

</manifest>